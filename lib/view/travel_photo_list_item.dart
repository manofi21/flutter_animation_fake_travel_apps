import 'package:flutter/material.dart';

class TravelPhotoListItem extends StatelessWidget {
  const TravelPhotoListItem({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}