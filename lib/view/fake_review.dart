import 'package:flutter/material.dart';

class FakeReview extends StatelessWidget {
  const FakeReview({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}